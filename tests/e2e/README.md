# End-to-end tests

Public API and route coverage is implemented by `scripts/smoke-test.mjs`.
Run against an explicit base URL:

```bash
SMOKE_BASE_URL=https://testing.greenleafpacific.com npm run smoke
```

Payment E2E remains unavailable until Windcave UAT inputs are installed.
