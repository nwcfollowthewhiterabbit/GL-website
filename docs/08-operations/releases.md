# Журнал релизов

> **NOT CONFIGURED.** Ниже только шаблон. Не создавайте фиктивную запись и не
> заполняйте неизвестные поля догадками.

Для каждого фактического развёртывания копируется
[шаблон release record](../templates/release-record.md). Запись неизменяемо
связывает исходники, runtime artifact, backup, проверки и способ возврата.

## Релизы

Фактических релизов пока не зафиксировано.

После первого подтверждённого развёртывания добавьте запись в обратном
хронологическом порядке. Наличие записи без evidence не подтверждает готовность.
