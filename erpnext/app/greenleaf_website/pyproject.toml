[project]
name = "greenleaf_website"
description = "Portable ERPNext layer for the Green Leaf storefront"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"
