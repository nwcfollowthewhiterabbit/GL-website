# Green Leaf Website

Minimal Frappe application containing only the ERPNext metadata and Desk page
required by the Green Leaf storefront.

Generated DocType and fixture files are built from the canonical definitions in
`erpnext/fixtures` by `npm run erpnext:build-app`.
